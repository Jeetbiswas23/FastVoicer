import React from "react";

const Payments = () => {
  return (
    <div className="min-h-screen bg-white px-6 py-10 text-gray-900">
      <h1 className="text-3xl font-semibold mb-6">Payments</h1>
      <p>No payment records available.</p>
    </div>
  );
};

export default Payments; // Ensure the component is exported as default